<%= form_with(model: alternativa, local: true) do |form| %>
  <% if alternativa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(alternativa.errors.count, "error") %> prohibited this alternativa from being saved:</h2>

      <ul>
        <% alternativa.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.text_field :question_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
