<p id="notice"><%= notice %></p>

<h1>Resolutions</h1>

<table>
  <thead>
    <tr>
      <th>Desc</th>
      <th>Questão</th>
      <th>Universitário</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @resolutions.each do |resolution| %>
      <tr>
        <td><%= resolution.desc %></td>
        <td><%= resolution.question.desc %></td>
        <td><%= resolution.universitario.username %></td>
        <td><%= link_to 'Show', resolution %></td>
        <td><%= link_to 'Edit', edit_resolution_path(resolution) %></td>
        <td><%= link_to 'Destroy', resolution, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Resolution', new_resolution_path %>
