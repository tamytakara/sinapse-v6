<p id="notice"><%= notice %></p>

<h1>Respostas</h1>

<table>
  <thead>
    <tr>
      <th>Resposta</th>
      <th>Gabarito</th>
      <th>Correta</th>
      <th>Questão</th>
      <th>Prova</th>
      <th>Vestibulando</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @answers.each do |answer| %>
      <tr>
        <td><%= answer.desc %></td>
        <td><%= answer.question.gabarito %></td>
        <td><%= answer.correta %></td>
        <td><%= answer.question.desc %></td>
        <td><%= answer.question.prova.name %> - <%= answer.question.prova.admission_exam.name %> </td>
        <td><%= answer.simulado.vestibulando.username %></td>
        <td><%= link_to 'Show', answer %></td>
        <td><%= link_to 'Edit', edit_answer_path(answer) %></td>
        <td><%= link_to 'Destroy', answer, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Answer', new_answer_path %>
